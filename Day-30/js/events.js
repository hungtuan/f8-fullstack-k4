// Định nghĩa sự kiện
// var inputList = document.querySelectorAll('[type="range"]');

// // var overEvent = new Event("over");
// var overEvent = new CustomEvent("over", { detail: "F8" });

// if (inputList.length) {
//   inputList.forEach((inputList) => {
//     inputList.addEventListener("mousemove", function (e) {
//       var value = (e.offsetX * 100) / this.clientWidth;
//       value = value.toFixed(2);
//       this.data = value;
//       this.dispatchEvent(overEvent);
//     });
//   });
// }
